// Dear Future Self — client configuration
//
// To run against a real Supabase project, fill these in. The values are
// PUBLIC — the anon key is safe to ship to the browser (RLS policies in
// the SQL migration protect user data).
//
// Leave them blank (empty strings) and the app will run in demo mode,
// storing capsules in your browser's localStorage only.

window.DFS_CONFIG = {
  supabaseUrl: "https://evrgrultzzmwdtxibgwq.supabase.co",
  supabaseAnonKey: "sb_publishable_54NcQZN3hjNDdQxEpE_QLw_EdTlaX8Q",
};
